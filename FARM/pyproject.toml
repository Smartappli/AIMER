[project]
name = "farm"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "flwr>=1.27.0",
    # "syft-flwr==0.5.1",
    "granian==2.5.0",
]

[tool.ruff]
target-version = "py313"

[tool.ruff.lint]
select = ["ALL"]
ignore = []
