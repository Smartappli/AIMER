[project]
name = "farm"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = [
    "django>=6.0.4",
    "flwr==1.30.0",
    "granian==2.7.4",
]

[tool.ruff]
target-version = "py313"

[tool.ruff.lint]
select = ["ALL"]
ignore = []
